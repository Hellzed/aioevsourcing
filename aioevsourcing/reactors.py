# base reactor, to trigger arbitrary command. Reactor are aware of context
# Two important specialised reactors: projector (to update state in Daily),
# and snapshot projector to optimize aggregate loading if needed.
